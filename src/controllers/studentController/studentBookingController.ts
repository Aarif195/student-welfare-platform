import { Request, Response } from "express";

export const getStudentBookingsController = (
  req: Request,
  res: Response
) => {};

export const getStudentBookingByIdController = (
  req: Request,
  res: Response
) => {};

export const bookRoomController = (
  req: Request,
  res: Response
) => {};

export const cancelBookingController = (
  req: Request,
  res: Response
) => {};

