import { Request, Response } from "express";

export const bookHostelController = (
  req: Request,
  res: Response
) => {};

export const cancelBookingController = (
  req: Request,
  res: Response
) => {};
