import { Request, Response } from "express";

export const getAllStudentsController = (
  req: Request,
  res: Response
) => {};

export const getAllOwnersController = (
  req: Request,
  res: Response
) => {};

export const getAllHostelsController = (
  req: Request,
  res: Response
) => {};

export const deleteStudentController = (
  req: Request,
  res: Response
) => {};

export const deleteOwnerController = (
  req: Request,
  res: Response
) => {};

export const approveHostelController = (
  req: Request,
  res: Response
) => {};

export const rejectHostelController = (
  req: Request,
  res: Response
) => {};
