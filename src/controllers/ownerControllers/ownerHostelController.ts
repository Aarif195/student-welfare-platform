import { Request, Response } from "express";

export const getMyHostelsController = (
  req: Request,
  res: Response
) => {};

export const getSingleHostelController = (
  req: Request,
  res: Response
) => {};

export const createHostelController = (
  req: Request,
  res: Response
) => {};

export const updateHostelController = (
  req: Request,
  res: Response
) => {};

export const deleteHostelController = (
  req: Request,
  res: Response
) => {};
