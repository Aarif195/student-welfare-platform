import { Request, Response } from "express";

export const ownerRegisterController = (
  req: Request,
  res: Response
) => {};

export const ownerLoginController = (
  req: Request,
  res: Response
) => {};
